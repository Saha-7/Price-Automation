# Azure SQL — set in Azure Portal → App Service → Configuration
db_serverendpoint=???.database.windows.net
db_zoho=db_Zoho_AMZ_API
db_returns=db_returns

# MongoDB
MONGODB_URI=mongodb+srv://???/shopify-prices

# Server
PORT=5000
NODE_ENV=production
SESSION_SECRET=some-random-string
FRONTEND_URL=https://your-frontend-url.com

# M365 OAuth (for login feature — set up later)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
OAUTH_REDIRECT_URI=