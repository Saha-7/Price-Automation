# ============================================================
# Set these in:
# Azure Portal → App Service → Configuration → Application Settings
# ============================================================

# ─── Azure SQL (required for fetchAndSave.js) ────────────────
db_serverendpoint=???.database.windows.net
db_zoho=db_Zoho_AMZ_API
db_returns=db_returns
db_userclientid=                    # Client ID of User-Assigned Managed Identity